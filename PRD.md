# 个人工作生活 App · 产品需求文档（PRD）

> 版本：v3.0
> 状态：已完成开发并上线
> 在线地址：

主站1:
https://sonder520.netlify.app/

备注:主站1因为部署次数的限制，没有更新新版本，请移步主站2和备用站。

主站2:
https://sonder520.pages.dev/

备用:
https://xiaoyu-hue.github.io/sonder520/

本文档随实现同步更新

> 运行环境：纯前端静态站点，无任何后端；数据存于访问者浏览器（localStorage 与 IndexedDB 双写双存，可选加密）

---

## 0. 版本历史与 v3.0 更新摘要

### 版本演进

| 版本 | 主题 |
| --- | --- |
| v1.0 | 初始化 Sonder：今日计划、快速备忘、自媒体、开发、咨询、阅读、新闻、设计、数据与设置 |
| v1.1 | 「水墨 × 液态玻璃」视觉语言：宣纸/墨黑双主题、自媒体发布统计、阅读统计、内容制作进度条 |
| v1.2 | 水墨微动态交互、自定义壁纸背景、侧栏液态玻璃片、全平台移动适配（平板图标栏/超小屏/横屏/dvh 回退） |
| v1.3 | 手机适配（底部玻璃导航/安全区/触控细节）、每日金句、娱乐游戏模块（井字棋/五子棋）、性能与兼容优化 |
| v2.0 | 游戏交互质量回归（三项修复）、五子棋移动端棋盘适配、自动化测试深化至 145 项、上线验收全绿 |
| v2.1 | 游戏/性能/PWA/安全/存储八项升级：棋盘震动修复、AI 难度分级、帧率档位、离线可用、零构建类型检查、全局搜索、XSS 加固、IndexedDB 双写；测试深化至 184 项 |
| **v3.0** | **可选加密、四款新迷你游戏、阅读计时与书摘、自媒体月历排期与数据图表、专注番茄钟、本周报告、壁纸上传、桌面通知、主题跟随系统、超宽屏适配、性能优化；测试深化至 213 项** |

### v3.0 更新摘要（本版）

1. **可选数据加密（隐私）**：存储层可选启用加密——PBKDF2（60 万次迭代）+ AES-GCM-256 密文写入 localStorage/IndexedDB；设置页启用向导、锁屏解锁、免密会话（本次会话内免重复输入）、支持加密备份文件导入时的密码输入；未启用时完全无感知（旧数据原样兼容）。
2. **今日计划 v3**：任务优先级升级为四档四色圆点（旧数据自动迁移）；「开始专注」悬浮 🍅 25 分钟倒计时，到时弹浏览器通知；今日完成率环形进度条实时更新。
3. **阅读计划 v3**：每本书累计阅读计时（分钟）与会话日志；摘抄金句（句子 + 页码）自动汇聚到独立「我的书摘」页（按书分组浏览）；标记「已读完」自动记录完成日期；首页每日金句位置有机会随机展示自己的书摘。
4. **自媒体 v3**：新增月历视图，选题可拖拽排期（桌面拖拽 / 移动端长按）；发布渠道下拉选择（公众号 / 小红书 / B 站 / 抖音）；已发布卡片可直接填写阅读量、点赞数；最近 5 篇阅读量 SVG 折线图。
5. **开发工作 v3**：技术笔记支持 Markdown 渲染与代码片段一键复制；笔记按更新时间倒序展示。
6. **数据与设置 v3**：一键生成「本周报告」（周一至周日的任务 / 阅读 / 随手记 / 选题汇总），可一键复制；新增桌面通知开关（今日任务提醒：未完成提醒 / 完成表扬、需授权）；壁纸上传入口（见下）；动画帧率档位（60/90/120）。
7. **娱乐游戏 v3**：新增 4 款迷你游戏——
   - 猜数字：7 次机会、大小提示、最佳纪录持久化；
   - 扫雷：三档难度、首击安全、标记模式、战绩持久化、格子实底与 clamp 防溢出修复；
   - 猜成语：谜面题库、3 次机会、提示字、战绩统计；
   - 脑筋急转弯：奇趣问答、看答案、战绩统计。
   - 四款新游戏战绩全部并入对战记录（solo 模式 + 胜负备注），记录页新增 AI 难度显示。
8. **自定义壁纸上传**：设置页上传背景图（≤2MB）——base64 持久化、超大图自动 canvas 压缩兜底、缩略图预览、恢复默认；渲染改为独立 img 元素 + object-fit cover，全机型/桌面统一铺满居中不拉伸。
9. **主题跟随系统**：主题自动跟随系统 `prefers-color-scheme`，手动选择可覆盖并记住。
10. **桌面通知提醒**：今日任务未完成提醒 / 完成表扬通知（设置开关 + 浏览器授权流程）。
11. **响应式全面适配**：桌面超宽屏（>1240px）内容整体限宽 1240px 居中；窄屏 `.row` 支持换行防溢出；新增 responsive 静态回归测试（720 底栏 / 平板图标栏 / 棋盘与格子 / 超小屏横屏兜底）。
12. **性能优化**：存储序列化去重、全局搜索索引缓存、困难难度五子棋前瞻剪枝；Service Worker 缓存版本升至 v12。
13. **测试基线**：新增全模块冒烟测试（补齐交互点击差集）、自动适配回归、加密/通知/新游戏等专项测试，`npm test` 由 184 项增至 **213 项全部通过**；继续保证全部 JS 通过 `node --check` 与 `npm run typecheck`。

### 历史版本要点（v2.1 / v2.0）

- **v2.1 八项升级**：落子动画零位移消除棋盘震动；井字棋/五子棋 AI 三档难度（简单/普通/困难）与设置持久化；动画帧率档位 60/90/120；PWA 离线可用（Service Worker + Manifest + 图标）；零构建 TypeScript 类型检查（JSDoc + tsc --noEmit）；顶栏全局搜索框（模糊匹配/分组/跳转高亮）；全模块 sanitize XSS 净化与危险 URL 协议白名单；IndexedDB 双写双存 + 超容量警示条与迁移；测试 184 项。
- **v2.0 七项修复**：双人模式悔棋响应与确认制；新开局/切模式彻底清盘；确认弹窗防叠加；五子棋手机端棋盘 `min(97vw,480px)` 方正适配（≥360px 级小屏可玩）；全机型适配复查；测试 145 项；文档与开源合规（MIT、行为准则、三站点同步说明、页脚声明）。

---

## 1. 产品目标和使用场景

### 产品目标
做个只供本人使用的工作与生活管理 App，把零散的个人工作（自媒体、开发、咨询、阅读、新闻、设计）和日常小事（今日计划、快速备忘）与休闲娱乐（六款小游戏）集中到一个本地工具里，减少多处切换、随手记录混乱的问题。

### 使用场景
- 电脑浏览器打开即用，快速查看今天要做什么。
- 临时有个想法，随手记入"快速备忘"。
- 每天按"今日计划"安排和处理任务，用 🍅 专注倒计时进入专注状态。
- 各工作模块分别记录进展，互不干扰，但首页可一览全局。
- 碎片时间在手机上玩五子棋、井字棋、猜数字、扫雷、猜成语或脑筋急转弯，战绩自动记录。

### 核心原则
- 纯前端实现：数据只保存在访问者自己的浏览器（localStorage）中，不上传任何服务器。
- 刷新、关闭标签页、关闭浏览器、重新启动后，数据都不能丢失。
- 不需要登录、云数据库、云同步，站点可离线打开运行。
- 中文界面，电脑 / 平板 / 手机浏览器均可使用。

---

## 2. 整体页面框架和导航结构

### 应用外壳
- **侧边导航**：应用名/Logo + 10 个导航入口，当前页高亮显示。
  - 首页总览、今日计划、自媒体、开发工作、咨询工作、阅读计划、看新闻计划、设计计划、娱乐游戏、数据与设置
  - 电脑：完整侧栏；平板（721–960px）：折叠为 70px 图标栏；手机竖屏（≤720px）：改为底部液态玻璃导航栏（可横向滑动、44px 触控目标、适配手势条安全区）。
- **顶部栏**：当前所在模块名称、当天日期、全局"＋"快捷新建按钮、全局"快速备忘"入口、全局搜索框。
- **内容区**：各模块独立渲染，切换导航时保留各页状态（娱乐游戏的对局状态切页后仍保留）。
- **弹窗统一规则**：新增/编辑/确认删除均使用居中浮层 + 半透明遮罩，按 Esc 可关闭；手机端为底部抽屉样式。
- **超宽屏**：桌面 >1240px 内容整体限宽 1240px 居中，大屏不摊开。

### 导航结构
```
侧边栏
├── 首页总览
├── 今日计划
├── 自媒体
├── 开发工作
├── 咨询工作
├── 阅读计划
├── 看新闻计划
├── 设计计划
├── 娱乐游戏
└── 数据与设置
```

---

## 3. 视觉与交互设计

- **主题**：水墨 × 液态玻璃。浅色「宣纸」/ 深色「墨黑」双主题，**默认跟随系统 `prefers-color-scheme`，手动选择可覆盖并记住**；磨砂玻璃卡片（backdrop-filter + 高光内边），朱砂红强调色，图表使用国画颜料色系。
- **自定义壁纸**：设置页可上传背景图（≤2MB，超大图自动压缩），调节透明度（0–100%，默认 40%），实时预览并持久化，可恢复默认；渲染使用独立 img 元素 + `object-fit: cover`，全机型铺满居中不拉伸。
- **微动态**：页面淡入错峰、图表水墨蔓延生长、按钮按压反馈、空状态呼吸、Toast 滑入、手机端弹窗底部抽屉。
- **性能优化**：移除 fixed 背景层级、磨砂降档适配（@supports 回退）、壁纸预加载、存储序列化去重、全局搜索索引缓存、困难五子棋前瞻剪枝——低端手机与老浏览器保证流畅。
- **无障碍**：尊重 `prefers-reduced-motion`；全键盘可达；图标按钮带 aria 标签与提示。
- **全平台适配**：
  - 电脑 >960px：完整侧栏 + 多列网格；>1240px 内容限宽居中
  - 平板 721–960px：侧栏为 70px 图标栏（隐藏文字）
  - 手机 ≤720px：底部液态玻璃导航
  - 超小屏 ≤360px：压缩留白不挤爆；`.row` 支持换行防溢出
  - 手机横屏：顶栏与导航自动变矮
  - 五子棋大棋盘 ≤720px：棋盘宽 `min(97vw,480px)`、格距 2px、内边距 8px、格子方形、棋子 86% 填充；≤360px 进一步压缩；棋盘始终不横向溢出（照顾 iPhone SE 一档小屏）
  - 安全区 `env(safe-area-inset-bottom)`、`100vh→100dvh` 回退（老 iOS/安卓）、触控目标 ≥44px、输入控件 16px 防 iOS 聚焦缩放、`color-scheme` 同步表单与滚动条
- **PWA 友好**：`viewport-fit=cover` + `apple-mobile-web-app-capable`，支持「添加到主屏幕」作为 App 打开；Service Worker 离线缓存（当前缓存版本 v12）。

---

## 4. 首页的内容结构

1. **顶部问候语 + 当天日期**；下方**每日金句**（库内按日期稳定轮换，每天 0 点自动换新；有机会随机展示自己的书摘；库内容缺失时优雅降级不报错）。
2. **今日任务摘要**：待办数/总数 + **今日完成率环形进度条**；可快捷打勾（不跳转页面）。
3. **快速备忘条**：直接输入即保存，最近一条常显在首页。
4. **各模块概览卡片区**（每模块一张数字卡片）：
   - 自媒体：选题数 / 待发布数
   - 开发工作：项目数 / 进行中任务数
   - 咨询工作：客户数 / 待跟进数
   - 阅读计划：在读数 / 总数
   - 看新闻计划：待读数 / 总数
   - 设计计划：项目数 / 进行中设计数
   - 娱乐游戏：对局数 / 胜平局
   - 点击任意卡片跳转对应模块。

---

## 5. 每个模块的用途

| 模块 | 用途 |
| --- | --- |
| 今日计划 | 管理每天要做的任务，按时间/优先级安排，专注倒计时，跟踪完成情况 |
| 快速备忘 | 任何时刻即点即存的文字记录，带保存时间，可查看历史 |
| 自媒体 | 管理内容创作：选题、月历排期、发布渠道、发布状态、阅读量/点赞与数据图表 |
| 开发工作 | 管理开发项目：任务、进度、Markdown 技术笔记 |
| 咨询工作 | 管理咨询客户与项目：客户档案、阶段、跟进记录、收入 |
| 阅读计划 | 管理书单：在读/想读/读完、阅读计时、读书笔记、书摘 |
| 看新闻计划 | 收藏想看的资讯：标题/链接/来源、待读/已读/收藏 |
| 设计计划 | 收集灵感、管理设计项目及阶段 |
| 娱乐游戏 | 井字棋 / 五子棋 / 猜数字 / 扫雷 / 猜成语 / 脑筋急转弯：AI 对决、双人对弈、悔棋、认输、自动战绩 |
| 数据与设置 | 统计总览、数据导出/备份/恢复、加密开关、主题与模块开关、通知与报告 |

---

## 6. 每个模块的数据和操作

### 今日计划
- 数据：任务标题、说明、优先级（四档四色，旧值自动迁移）、计划日期、完成状态、完成时间、排序、专注会话日志。
- 操作：新增、编辑、删除（二次确认）、勾选完成/恢复、调整顺序、**开始专注（🍅 25 分钟悬浮倒计时，到时浏览器通知）**。

### 快速备忘
- 数据：备忘内容、保存时间。
- 操作：即点即存、历史查看、归档。

### 自媒体
- 数据：选题/内容标题、平台账号、发布渠道（公众号/小红书/B 站/抖音）、标签、状态（草稿/待发布/已发布）、发布日期、阅读量/点赞、备注、进度。
- 操作：新增、编辑、筛选（标签/状态）、改状态、详情、导出 CSV、进度拖拽条、**月历视图拖拽排期（桌面拖拽/移动端长按）**、**最近 5 篇阅读量 SVG 折线图**、发布数据可视化。

### 开发工作
- 数据：项目名称、项目任务、进度、技术笔记（**Markdown 渲染、代码片段一键复制**）、完成统计。
- 操作：新增项目、项目内新增/勾选任务、写技术笔记（按更新时间倒序）、查看统计。

### 咨询工作
- 数据：客户档案、客户项目、项目阶段、跟进记录、待办、收入记录。
- 操作：新增客户、客户详情内管理项目/跟进/收入。

### 阅读计划
- 数据：书名、作者、状态（在读/想读/读完）、阅读进度、**累计阅读分钟与会话日志**、笔记摘录、**书摘（句子 + 页码，独立「我的书摘」页按书分组）**、完成日期。
- 操作：新增书、改状态、更新进度、**开始/结束阅读计时**、写摘录与书摘、按状态浏览、阅读量统计。

### 看新闻计划
- 数据：标题、链接、来源、标签、状态（待读/已读/收藏）。
- 操作：新增、点击打开链接、标记已读、收藏、按标签浏览。

### 设计计划
- 数据：灵感记录（链接/想法）、灵感分类、设计项目、项目阶段（构想/进行/定稿）。
- 操作：新增灵感、灵感分类浏览、新增项目、推进阶段。

### 娱乐游戏
- 数据：对局记录（游戏/模式/胜者/是否认输/**AI 难度**/备注/时间）、战绩汇总（对局数/胜/平）。
- 操作：
  - 选游戏：井字棋 3×3 / 五子棋 15×15 / 猜数字 / 扫雷 / 猜成语 / 脑筋急转弯。
  - 对弈类（井字棋/五子棋）：模式 AI 对决（可执先手或后手）/ 双人对弈；三档 AI 难度（简单/普通/困难）持久化。
  - 落子：点击棋盘落子，AI 模式轮到 AI 时自动应手（首步延迟 320ms、后续 220ms，模拟思考节奏）。
  - 悔棋（AI 模式）：即点即悔无需确认，可连续悔多回合；AI 正在思考时悔棋会取消其待落子。
  - 悔棋（双人模式）：由刚落子的一方提出收回刚落的这一步，对方确认后才撤销；对局结束后不可悔棋。
  - 认输：二次确认，判对方胜；AI 模式判给 AI 获胜。
  - 新开局：有落子时二次确认并清空棋盘；连点新开局/切换模式不会弹多个确认框。
  - 迷你游戏：猜数字（7 次机会/大小提示/最佳纪录）、扫雷（三档难度/首击安全/标记模式）、猜成语（3 次机会/提示字）、脑筋急转弯（看答案/奇趣问答）；战绩自动记录。
  - 返回选游戏：当前局未结束时二次确认；清空战绩二次确认。
- AI 行为：井字棋为完全搜索（必不输）；五子棋为评分启发式（主动五连优先、封堵对手冲四/活四、进攻权重高于防守，困难档带一层前瞻）。
- 对局切换导航后状态保留，回来可继续下；战绩自动写入本地存储，首页概览与数据页统计同步。

### 数据与设置
- 数据：应用设置（主题、模块开关、动画帧率、通知开关、加密状态）、统计数据。
- 操作：切换主题（跟随系统/手动）、开关模块、**启用/停用加密（向导 + 锁屏 + 免密会话）**、**上传/恢复壁纸、调透明度**、**生成本周报告（一键复制）**、**桌面通知授权**、导出备份（未加密或加密密码）、导入恢复、查看统计、清空战绩、数据迁移（IndexedDB）。

---

## 7. 首页、今日计划和各模块之间的关系

- **今日计划**是每日任务的核心执行层；首页只展示其摘要并支持快捷打勾。
- **快速备忘**是全局随手记录入口；首页显示最近一条，完整历史在各模块/备忘录中。
- **各工作模块**是独立的记录单元；首页通过概览卡片汇总每个模块的关键数字，点击卡片进入。
- **娱乐游戏**为休闲模块：对局状态切换导航后保留（返回游戏页可继续），战绩计入首页与数据页统计。
- **数据与设置**控制全局外观、模块可见性以及数据备份恢复，不参与日常业务数据。
- 数据统一存储，各模块之间不强制关联；今日计划与工作模块不做自动联动。

---

## 8. 本地数据、备份和恢复要求

- **数据存储**：数据保存在当前浏览器内置的本地存储（localStorage）中，使用 JSON 结构；同时写入 IndexedDB 双写双存（容量约 5MB → 远超），启动时自动从较新版恢复并回填。
- **加密**：设置页可选启用存储加密（PBKDF2 60 万次 + AES-GCM-256）；启用后数据以密文存储，解锁密码只存在浏览器内存，不保存密码本身。
- **持久性要求**：刷新页面、关闭标签页、关闭并重新打开浏览器后，数据不丢（已有全量回归覆盖）。
- **备份**：在"数据与设置"中提供"导出备份"，将全部数据下载为本地 JSON 文件（加密状态下需输入解锁密码，导出文件为密文）。
- **恢复**：在"数据与设置"中提供"导入恢复"，选择备份文件恢复全部数据（导入前提示会覆盖现有数据；加密备份需输入密码）。
- **说明**：备份文件由用户自行保存；不提供自动云备份；多设备数据各自独立，迁移用导出/导入。

---

## 9. 本版完成的功能

1. 侧边栏 + 顶部栏整体框架，10 个页面可切换，各页状态保留；顶栏全局搜索框（实时模糊匹配/分组/跳转高亮）。
2. 手机底导 + 平板图标栏 + 桌面超宽屏（>1240px 限宽居中）全平台适配；安全区 / dvh / 触控 / 防缩放全齐。
3. 主题（宣纸/墨黑，跟随系统可覆盖）、自定义壁纸上传与透明度（0–100%）、磨砂玻璃、微动态，老机型性能与降级。
4. 每日金句按日轮换（有机会展示自己的书摘），库缺失优雅降级。
5. 首页：问候、今日摘要 + 完成率环形进度条、快速备忘、各模块概览卡片、卡片跳转。
6. 今日计划：增删改/排序/勾选/分组、四档优先级四色圆点、🍅 专注 25 分钟悬浮倒计时（到时浏览器通知）。
7. 一键备忘：即存即取、历史、归档。
8. 六个内容模块，各自增/删/改/筛选/统计；自媒体导出 CSV + 月历拖拽排期 + 渠道 + 阅读量折线图；开发笔记 Markdown 渲染与代码复制；阅读计时与「我的书摘」页；咨询收入记录。
9. 娱乐游戏六款：井字棋 / 五子棋（AI 三档难度 / 双人，悔棋认输开局确认制，全规则回归锁定）+ 猜数字 / 扫雷 / 猜成语 / 脑筋急转弯（战绩并入对战记录，含 solo 模式与备注、AI 难度显示）。
10. 数据与设置：导出/导入/统计/清空战绩/主题/模块开关/动画帧率（60/90/120）/数据迁移（IndexedDB）/加密（向导+锁屏+免密会话）/桌面通知/本周报告一键复制/壁纸上传。
11. 统一交互：弹窗规则、二次确认、Esc 关闭、Toast、确认框防叠。
12. 可靠性：PWA 离线可用（缓存 v12）、存储双写双存、存储超 4.5MB 顶部警示条、XSS 全模块净化、可选加密、性能优化（序列化去重/搜索索引缓存/AI 剪枝）。
13. 自动化测试 213 项全绿（两轮全量验证）。

---

## 10. 明确不做 / 后续考虑的功能

- 登录、用户体系、权限、多人实时在线对战。
- 云同步、云数据库、多设备数据互通。
- 联网自动抓取新闻及任何联网功能；推送通知（仅限浏览器内桌面通知）。
- 今日计划与内容模块的自动联动。
- 国际化（多语言界面）——考虑中，尚未实现。

---

## 11. 验收标准（可实际检查）

1. 首页：问候语、日期、金句、今日摘要、完成率环形、备忘条、概览卡片全部正常。
2. 点卡片跳转对应模块；点导航可返回。
3. 今日计划/备忘及各模块核心操作可用且互不干扰；阅读进度、自媒体进度可实时。
4. 专注倒计时：开始后悬浮显示 25 分钟倒计时，到时弹浏览器通知（需授权）。
5. 阅读：开始阅读计时累计分钟；书摘页按书分组展示；标记读完自动记完成日期；首页金句位有机会看到自己的书摘。
6. 自媒体：月历视图可拖拽/长按选题排期；已发布卡片可填阅读量/点赞并出现在折线图。
7. 加密：设置启用向导 → 锁屏解锁 → 数据为密文 → 导出导入加密备份需密码；未启用时无感。
8. 娱乐游戏：六款游戏均可完成一局并把战绩记入对战记录（含 solo 模式、备注、AI 难度）；五子棋/井字棋的悔棋、认输、新开局均按规则跑通；终局后不能悔棋；连续点「新开局」只出现一个确认框。
9. 五子棋手机端：375px 宽手机打开为正方形格子、刚好在可视宽度内，无变形、无横向滚动；360px 级小屏仍可正常落子。
10. 超宽屏：桌面 2560px 宽打开，内容居中限宽 1240px，不左右摊开。
11. 平板 760px：侧栏收为图标栏；手机竖屏：底部导航+抽屉弹窗；横屏顶栏变矮。
12. 主题跟随系统切换（可手动覆盖）；壁纸上传生效、可调透明度、可恢复默认。
13. 导出 JSON → 修改数据 → 导入恢复，数据与导出时一致（导入前有覆盖提示）。
14. 持久化：任意数据录入后刷新、关标签、重启浏览器均不丢失。
15. 手机外链、离线打开本地 index.html 均可用；数据只在本地浏览器中。

---

## 12. 技术架构与质量保障

- **实现**：纯 HTML + CSS + 原生 JS，零构建、零运行时依赖；全部资源为相对路径，可部署到任意静态托管（Netlify / Cloudflare Pages / Vercel / GitHub Pages），也可直接双击打开本地文件。
- **代码组织**：
  - 数据层：`store.js`（localStorage + IndexedDB 双写双存、字段归一化、统计聚合、容量警示与迁移）+ `encryption.js`（可选加密核心：PBKDF2 60 万次 + AES-GCM-256，密文分支与锁屏）
  - 组件：`ui.js`（弹窗/Toast/表单、统一 sanitize 净化与 URL 协议白名单）
  - 模块：`home / today / memo / selfmedia / dev / consulting / reading / news / design / settings` 各一个文件
  - 游戏：`games-logic.js`（纯对弈规则与三档难度 AI 策略：井字棋完全搜索、五子棋启发式评分 + 困难档前瞻剪枝；猜数字/扫雷/猜成语/脑筋急转弯规则与题库）+ `games.js`（游戏视图、悔棋/认输/开局/战绩、AI 落子节奏）
  - 搜索：`search.js`（全局索引 + 缓存、模糊匹配、分组跳转、水墨高亮定位）
  - 扩展渲染：`markdown.js`（技术笔记 Markdown 渲染与代码复制）
  - 壳层：`app.js`（路由/主题跟随/帧率档位/警示条）、`sw.js`（Service Worker 离线缓存，当前 v12）、`manifest.json`（PWA 清单与图标）
- **主题系统**：CSS 自定义属性 + 深浅双主题；`@supports` 为不支持 `backdrop-filter` 的浏览器降低层级；壁纸独立 img 元素 + `object-fit: cover` + 透明度变量。
- **数据兼容**：读取时做字段归一化（缺失补默认、非法值夹取），支持历史版本数据平安迁移；IndexedDB 与 localStorage 双写双存，冲突按保存时间取新；加密开关对旧数据完全兼容（未启用时无感）。
- **移动端棋盘细节**：15 列盘使用 grid；`.game-board.big` 手机断点压缩格距与边距、格子 `min-height: 0` 保持方正 1:1；宽度 `min(97vw, 480px)` 保证不溢屏；棋盘格子为游戏固有热区（整盘须尽收眼底，小屏约 21px/格），其余全部 UI 触控目标 ≥44px。
- **自动化测试**：jsdom + `node --test`；覆盖存储、加密、UI、全部模块、样式、动效、壁纸、移动端自动适配（responsive 静态回归）、性能、游戏引擎（胜负检测/禁手逻辑/AI 策略/新四款游戏）、交互质量回归（确认弹窗防叠、悔棋语义、终局锁定、手机棋盘压缩）、PWA、搜索、XSS 净化、IndexedDB 双写与容量警示、通知，当前 **213 项全部通过**（`npm test` 连续两轮全绿）。
- **持续质量**：每轮改动后执行源码 `node --check` 语法检查、`npm run typecheck`（零构建 TypeScript 类型检查）与全量测试。
