# 个人工作生活 App · 产品需求文档（PRD）

> 版本：v1.2
> 状态：已完成开发并上线（https://sonder520.netlify.app/），本文档随实现同步更新
> 运行环境：纯前端静态站点，无任何后端；数据存于访问者浏览器 localStorage

---

## 1. 产品目标和使用场景

### 产品目标
做一个只供本人使用的工作与生活管理 App，把零散的个人工作（自媒体、开发、咨询、阅读、新闻、设计）和日常生活（今日计划、快速备忘）集中到一个本地工具里，减少在多处切换、随手记录混乱的问题。

### 使用场景
- 日常在电脑浏览器中打开即用，快速查看今天要做什么。
- 临时有个想法，随手记入"快速备忘"。
- 每天按"今日计划"安排和处理任务。
- 各工作模块（自媒体/开发/咨询/阅读/新闻/设计）分别记录自己的进展，互不干扰，但首页可一览全局。

### 核心原则
- 纯前端实现：数据只保存在访问者自己的浏览器（localStorage）中，不上传任何服务器。
- 刷新、关闭标签页、关闭浏览器、重新启动后，数据都不能丢失。
- 不需要登录、云数据库、云同步，站点可离线打开运行。
- 中文界面，电脑 / 平板 / 手机浏览器均可使用。

---

## 2. 整体页面框架和导航结构

### 应用外壳
- **左侧固定侧边栏**：应用名/Logo + 10 个导航入口，当前页高亮显示。
  - 首页总览、今日计划、自媒体、开发工作、咨询工作、阅读计划、看新闻计划、设计计划、娱乐游戏、数据与设置
  - 电脑：完整侧栏；平板：折叠为 70px 图标栏；手机竖屏：改为底部玻璃导航栏。
- **顶部栏**：当前所在模块名称、当天日期、全局"＋"快捷新建按钮、全局"快速备忘"入口。
- **内容区**：各模块独立渲染，切换导航时保留各页状态。
- **弹窗统一规则**：新增/编辑/确认删除均使用居中浮层 + 半透明遮罩，按 Esc 可关闭（手机端为底部抽屉样式）。

### 导航结构
```
侧边栏
├── 首页总览
├── 今日计划
├── 自媒体
├── 开发工作
├── 咨询工作
├── 阅读计划
├── 看新闻计划
├── 设计计划
├── 娱乐游戏
└── 数据与设置
```

---

## 3. 视觉与交互设计

> v1.1 起在原有功能型基础上确立整体视觉语言。

- **主题**：水墨 × 液态玻璃。浅色「宣纸」/ 深色「墨黑」双主题（跟随系统与手动切换），磨砂玻璃卡片（backdrop-filter + 高光内边），朱砂红作为强调色，图表使用国画颜料色系。
- **自定义壁纸**：设置页可上传/更换背景图并调节透明度（0–100%，默认 40%），实时预览并持久化。
- **微动态**：页面淡入错峰、图表墨液蔓延生长、按钮按压反馈、空状态呼吸、Toast 滑入、弹窗手机端底部抽屉。
- **无障碍与细节**：尊重 `prefers-reduced-motion`；全键盘可达；图标按钮带 aria 标签与提示。
- **移动适配**：安全区（刘海/手势条）、`100dvh` 动态高度回退、触控目标 ≥44px、输入框 16px 防聚焦缩放、支持「添加到主屏幕」作为 App 打开。

---

## 4. 首页的内容结构

1. **顶部问候语 + 当天日期**；问候语下方显示**每日金句**（库内按日期稳定轮换，每天 0 点自动换新）。
2. **今日任务摘要**：显示待办数/总数，可快捷打勾（不用跳转页面）。
3. **快速备忘条**：直接输入并保存，最近一条常显在首页。
4. **各模块概览卡片区**（每模块一张数字卡片）：
   - 自媒体：选题数 / 待发布数
   - 开发工作：项目数 / 进行中任务数
   - 咨询工作：客户数 / 待跟进数
   - 阅读计划：在读数 / 总数
   - 看新闻计划：待读数 / 总数
   - 设计计划：项目数 / 进行中数
   - 娱乐游戏：对局数 / 胜平局
   - 点击任意卡片跳转到对应模块。

---

## 5. 每个模块的用途

| 模块 | 用途 |
| --- | --- |
| 今日计划 | 管理每天要做的任务，按时间/优先级安排，跟踪完成情况 |
| 快速备忘 | 任何时刻即点即存的文字记录，带保存时间，可查看历史 |
| 自媒体 | 管理内容创作：选题、发稿计划、账号、发布状态 |
| 开发工作 | 管理开发项目：任务、进度、技术笔记 |
| 咨询工作 | 管理咨询客户与项目：客户档案、跟进记录、收入 |
| 阅读计划 | 管理书单：在读/想读/读完、进度、读书笔记 |
| 看新闻计划 | 收藏想看的资讯：标题/链接/来源、待读/已读/收藏 |
| 设计计划 | 收集灵感、管理设计项目及阶段 |
| 娱乐游戏 | 井字棋 / 五子棋对战：AI 对决、双人对弈、悔棋、认输、自动战绩 |
| 数据与设置 | 统计总览、数据导出/备份/恢复、主题与模块开关 |

---

## 6. 每个模块第一版最必要的数据和操作

### 今日计划
- 数据：任务标题、说明、优先级、计划日期、完成状态、完成时间、排序。
- 操作：新增、编辑、删除（二次确认）、勾选完成/恢复、调整优先级顺序。

### 快速备忘
- 数据：备忘内容、保存时间。
- 操作：即点即存、查看历史、归档。

### 自媒体
- 数据：选题/内容标题、平台/账号、标签、状态（草稿/排队/已发布）、发布日期、备注。
- 操作：新增、编辑、筛选（标签/状态）、改状态、详情查看、导出 CSV。

### 开发工作
- 数据：项目名称、项目任务、进度、技术笔记、完成统计。
- 操作：新增项目、项目内新增/勾选任务、写技术笔记、查看统计。

### 咨询工作
- 数据：客户档案、客户项目、项目阶段、跟进记录、待办、收入记录。
- 操作：新增客户、客户详情内管理项目/跟进/收入。

### 阅读计划
- 数据：书名、作者、状态（在读/想读/读完）、阅读进度、笔记摘录。
- 操作：新增书、改状态、更新进度、写摘录、按状态浏览。

### 看新闻计划
- 数据：标题、链接、来源、标签、状态（待读/已读/收藏）。
- 操作：新增、点击打开链接、标记已读、收藏、按标签浏览。

### 设计计划
- 数据：灵感记录（链接/想法）、灵感分类、设计项目、项目阶段（构想/进行/定稿）。
- 操作：新增灵感、灵感分类浏览、新增项目、推进阶段。

### 娱乐游戏
- 数据：对局记录（游戏/模式/胜者/是否认输/时间）、战绩汇总（对局数/胜/平）。
- 操作：选择游戏（井字棋 3×3 / 五子棋 15×15）、AI 对决（可执先手或后手）/ 双人对弈、落子、悔棋（AI 模式可连续悔棋，每次回退己方与 AI 各一步回到思考点，AI 思考中悔棋会取消其待落子；双人模式悔棋仅对局进行中可用、由刚落子的一方提出收回自己刚落的这步、对方同意才撤销）、认输（二次确认，判对方胜）、新开局（有落子时二次确认并清空棋盘，连点不会叠加弹窗）、返回选游戏、清空战绩。
- AI 行为：井字棋为完全搜索（必不输）；五子棋为评分启发式（主动成五连优先、封堵对手冲四/活四、进攻权重略高）。
- 统计：战绩自动写入 localStorage，首页概览与数据页统计同步。

### 数据与设置
- 数据：应用设置（主题、模块开关）、统计数据。
- 操作：切换主题、开关模块、导出备份、导入恢复、查看统计图表。

---

## 7. 首页、今日计划和各模块之间的关系

- **今日计划**是每日任务的核心执行层；首页只展示它的摘要并支持快捷打勾。
- **快速备忘**是全局随手记录入口；首页显示最近一条，完整历史在各模块/备忘录中。
- **各工作模块**是独立的记录单元；首页通过概览卡片汇总每个模块的**关键数字**（见第 3 节），点击卡片跳转进入。
- **娱乐游戏**为休闲模块：对局状态在切换导航后仍保留（返回游戏页可继续），战绩计入首页与数据页统计。
- **数据与设置**控制全局外观、模块可见性以及数据备份恢复，不参与日常业务数据。
- 数据统一存储，各模块之间不强制关联；今日计划与工作模块不做自动联动（第一版）。

---

## 8. 本地数据文件、备份和恢复要求

- **数据存储**：数据保存在当前电脑浏览器内置的本地存储（localStorage）中，使用 JSON 结构。
- **持久性要求**：刷新页面、关闭标签页、关闭并重新打开浏览器后，数据均不丢失。
- **备份**：在"数据与设置"中提供"导出备份"，将全部数据下载为本地 JSON 文件。
- **恢复**：在"数据与设置"中提供"导入恢复"，选择备份文件可恢复全部数据（导入前提示会覆盖现有数据）。
- **说明**：备份文件由用户自行保存；不提供自动云备份。

---

## 9. 第一版必须完成的功能

1. 左侧导航 + 顶部栏的整体框架，9 个页面可切换。
2. 首页总览：问候语、今日摘要、快速备忘条、六模块概览卡片、卡片跳转。
3. 今日计划：新增/编辑/删除/勾选完成/调整顺序，过期与完成分组展示。
4. 快速备忘：即点即存、历史查看、归档。
5. 自媒体：选题与发稿列表、标签与状态筛选、详情与状态修改、导出 CSV。
6. 开发工作：项目列表、项目任务、进度、技术笔记。
7. 咨询工作：客户档案、客户项目、跟进记录、收入记录。
8. 阅读计划：书单三状态、阅读进度、读书笔记。
9. 看新闻计划：资讯收藏、标签、待读/已读/收藏、跳转链接。
10. 设计计划：灵感收集、灵感分类、设计项目与阶段。
11. 数据与设置：主题切换、模块开关、导出备份、导入恢复、统计图表。
12. 统一交互：弹窗规则、删除二次确认、空状态引导、Esc 关闭。
13. 数据持久化，满足刷新/关闭/重启不丢失。

---

## 10. 当前版本已上线，明确不做/后续考虑的功能

- 登录、用户体系、权限。
- 云同步、云数据库、多设备数据互通（各设备数据相互独立）。
- 联网自动抓取新闻或任何联网功能。
- 复杂的定时提醒推送（如浏览器系统级通知）。
- 多用户/共享协作。
- 今日计划与各工作模块之间的自动联动。

---

## 11. 验收标准（可实际检查）

1. 打开首页，问候语、今日日期、今日摘要、快速备忘条、各模块概览卡片均正常显示。
2. 点击概览卡片能跳转到对应模块，点击导航能返回首页。
3. 今日计划：可新增任务、编辑、删除（有确认弹窗）、勾选完成、调整顺序；过期任务有标识；刷新后数据仍在。
4. 快速备忘：输入即保存，首页显示最近一条，历史列表可见，可归档。
5. 六个工作模块各自的新增、编辑、状态修改、筛选等核心操作均可用且相互独立。
6. 自媒体模块可导出 CSV 文件并正常打开。
7. 看新闻模块点击资讯可打开对应链接（浏览器新标签页）。
8. 数据与设置：切换主题立即生效；关闭某模块后侧边栏不再显示；统计图表数据与各模块数据一致。
9. 备份：点击导出下载 JSON 文件；修改数据后通过导入恢复，数据与导出时一致（导入前有覆盖提示）。
10. 持久化验证：完成任意数据录入后，刷新页面、关闭标签页、完全关闭并重启浏览器，数据均不丢失。
11. 所有删除操作均需二次确认，空状态有引导提示，弹窗可按 Esc 关闭。
12. 切换浅色/深色主题立即生效；设置自定义壁纸透明度后背景实时变化且刷新后保持。
13. 手机浏览器打开：底部导航可切换全部页面；在 iPhone 刘海屏/手势条设备上内容不被遮挡；支持直接打开本地文件运行（离线可用）。

---

## 12. 技术架构与质量保障

- **实现**：纯 HTML + CSS + 原生 JS，零构建、零运行时依赖；全部资源为相对路径，可部署到任意静态托管（Netlify / GitHub Pages / Cloudflare Pages / Vercel），也可直接用浏览器打开本地文件。
- **代码组织**：`store.js`（数据层：localStorage 读写、字段归一化、统计聚合）、`ui.js`（弹窗/Toast/表单组件）、各模块一个 JS 文件（home/today/memo/selfmedia/dev/consulting/reading/news/design/settings）、`app.js`（外壳与导航）。
- **主题系统**：CSS 自定义属性 + 深浅双主题变量；`@supports` 为不支持 `backdrop-filter` 的浏览器提供降级；壁纸层独立于玻璃层并支持透明度变量。
- **数据兼容**：数据读取时做字段归一化（缺失字段补默认值、非法值夹取范围），便于未来版本安全迁移。
- **自动化测试**：基于 jsdom 的 Node 测试（`node --test`），覆盖存储、UI、各模块、样式、动效、壁纸、移动端适配、性能、游戏引擎与交互质量回归，当前 144 项全部通过（`npm test`）。
