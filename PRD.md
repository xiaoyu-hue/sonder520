# 个人工作生活 App · 产品需求文档（PRD）

> 版本：v6.0（规划中，工程基线）
> 状态：已完成开发并上线（v5.2 为最新发布版；v6.0 为渐进式框架演进，见 CHANGELOG）
> 在线地址：

主站1:
https://sonder520.netlify.app/

备注:主站1因为部署次数的限制，没有更新新版本，请移步主站2和备用站。

主站2:
https://sonder520.pages.dev/

备用:
https://xiaoyu-hue.github.io/sonder520/

本文档随实现同步更新

> 运行环境：纯前端静态站点，无任何后端；数据存于访问者浏览器（localStorage 与 IndexedDB 双写双存，可选加密）

---

## 0. 版本历史与 v5.0 更新摘要

### 版本演进

| 版本 | 主题 |
| --- | --- |
| v1.0 | 初始化 Sonder：今日计划、快速备忘、自媒体、开发、咨询、阅读、新闻、设计、数据与设置 |
| v1.1 | 「水墨 × 液态玻璃」视觉语言：宣纸/墨黑双主题、自媒体发布统计、阅读统计、内容制作进度条 |
| v1.2 | 水墨微动态交互、自定义壁纸背景、侧栏液态玻璃片、全平台移动适配（平板图标栏/超小屏/横屏/dvh 回退） |
| v1.3 | 手机适配（底部玻璃导航/安全区/触控细节）、每日金句、娱乐游戏模块（井字棋/五子棋）、性能与兼容优化 |
| v2.0 | 游戏交互质量回归（三项修复）、五子棋移动端棋盘适配、自动化测试深化至 145 项、上线验收全绿 |
| v2.1 | 游戏/性能/PWA/安全/存储八项升级：棋盘震动修复、AI 难度分级、帧率档位、离线可用、零构建类型检查、全局搜索、XSS 加固、IndexedDB 双写；测试深化至 184 项 |
| v3.0 | 可选加密、四款新迷你游戏、阅读计时与书摘、自媒体月历排期与数据图表、专注番茄钟、本周报告、壁纸上传、桌面通知、主题跟随系统、超宽屏适配、性能优化；测试深化至 213 项 |
| v4.0 | 质量加固体系（23 项）：契约防御（类型/测试/白名单审计）、eslint 防线、错误上报、XSS 属性注入修复、加密 5 项修复、焦点管理与对比度、store 领域拆分、脚本清单收敛；测试深化至 407 项 |
| **v5.0** | **两轮评审修复 + 一轮补丁（16 项）：统计语义、CSV 注入、扫雷死局、认输归属、渲染副作用、引擎校验、存储韧性、搜索索引、对比度 token、壳降级、测试 flake 加固、扫雷首击插旗暂存、终局认输防重记、AI 重入守卫、撤销不顶替当前页；测试深化至 423 项** |
| **v5.1** | **性能与可靠性四补丁：PBKDF2 派生密钥缓存（解锁提速）、SonderBus 事件总线（跨模块解耦自动刷新）、五子棋 AI 异步化（Web Worker + 同步兜底 + 过期应手丢弃）、持久化危机兜底（双写失效红色警示条强制引导导出）；测试深化至 451 项** |
| **v5.2** | **工程防线补强 + 可靠性（9 项）：表单注入转义（data-k 属性 + getAttribute 匹配）、waitFor 条件轮询（消除固定延时竞态）、测试超时硬性兜底（npm 30s + CI 10min）、__SONDER_TEST__ 门闩（测试钩子隔离）、死代码清理、重复逻辑收敛（num0/esc/hashStr/copyText）、弱覆盖域页面交互补测 29 项、CI Node 20 弃用警告消除、PWA 导航 Network First（刷新即拿新版，离线仍回退）；测试深化至 514 项** |
| **v6.0** | **Sonder-Frame 渐进式框架（规划中，未发布）：TrustLayer 结构化存储状态（getStorageStatus/persistResult/diagnostics）+ IndexedDB 优先写（主快照反转，LS 降级副本）+ 集合级持久化（ADR-009 决策 7：逐集合独立 key 读写/单集合增量写/legacy 整份一次性迁移保旧键/加密逐集合独立 bundle）+ 标准模块工厂（ModuleFactory CRUD/Schema/净化/注册表，prepend/timeField/orderField/move 扩展）+ EventBridge 事件契约（EVENT 常量表）+ 试点迁移（memo/today/dev/news/selfmedia/consulting/reading/design 八个标准模块全部入厂——Phase 7 标准模块收官，嵌套边界/委托绑定收敛/最大模块压测/实时状态模块例外/业务字段 select 白名单归一）+ 离线状态指示器；测试基线 514 → 592 项** |

### v5.0 更新摘要（本版）

1. **统计语义修正**：设置页完成率分母改为「今日到期任务」（已完成 + 待办 + 过期），不再被全量历史数据稀释。
2. **CSV 注入防护**：自媒体导出 CSV 时对以 = + - @ 开头的字段加防注入前缀，避免公式注入。
3. **扫雷死局修复**：首击后若周边全为空仍可继续展开；严格边界校验（拒绝 NaN/小数坐标），补真实首击/右键首击/完整对局回归测试。
4. **认输归属修正**：认输后胜者归属按规则正确判定（含 AI 模式判 AI 胜），与战绩记录一致。
5. **渲染副作用收敛**：纯渲染路径不再写 store（改 render 副作用为显式提交），切页/重渲染不产生脏数据。
6. **游戏引擎校验**：开局前对棋盘/越界/非法参数做统一校验，杜绝引擎层抛异常被吞。
7. **存储韧性（review2）**：`_idbWrite` 拒绝空参删除明文回退；`_hasEncSnapshot/_decryptParse` 对未知加密版本（如未来 v2 密文）失败关闭走锁定流，密文不被明文覆盖；`migrateToIdb` 增加锁定守卫。
8. **搜索索引补全（review2）**：书摘、娱乐游戏（战绩）加入全局索引并补类别中文名；dev 任务条目 id 改用任务 id。
9. **对比度 token 化（review2）**：新增 `--warn-text/--ok-text/--accent-text` 双主题 token（浅色 5 处文本、扫雷数字、深色同步补亮），全部 ≥4.5:1（对照扫雷格子底色实测）。
10. **壳层健壮性（review2）**：`buildNav` 逐模块 try/catch 降级，单个模块异常不再导致整个外壳崩溃。
11. **测试 flake 加固（review2）**：AI 应手固定延时等待改轮询（±50ms 余量）；扫雷胜利断言改 `setMineField` 确定性雷位（原随机洪泛可能被静默跳过）；消除 2 处随机格子选择。
12. **测试基线**：新增 7 项回归（v2 密文韧性/空参守卫/锁定迁移/完成率分母/书摘与战绩索引/对比度 token/扫雷数字），`npm test` 407 → **422 项全部通过**；tsc/lint 0。

### v5.0 补丁摘要（P5a，紧随其后）

13. **扫雷首击插旗暂存**：空白阶段首次动作是插旗时不布雷（布雷推迟到首次翻开），旗位先渲染、翻开时回填——先插旗再翻首击依然必安全，修复了旧行为「右键即布雷、可能瞬间踩雷」。
14. **终局认输防重记**：`resign` 返回 null（对局已结束，如幽灵定时器已落子）时不再弹确认框、不追加战绩——消除双重记录。
15. **AI 应手重入守卫**：`aiThink` 已挂起定时器时直接跳过，render 恢复路径与落子路径不再各自调度出重复应手。
16. **撤销不顶替当前页**：memo / reading（书摘）/ today 三处撤销 confirm 改为「仍停留在原页才重渲染该页视图，已切页则仅提示已恢复」——修复旧行为「#content 常驻导致撤销整页渲染顶替已切换的页面」。
17. **测试基线**：新增「终局认输不再追加战绩」回归，`npm test` **423 项全部通过**；tsc/lint 0。

### v5.1 性能与可靠性补丁摘要（本版）

1. **PBKDF2 派生缓存**：`encryption.js` 增加派生密钥缓存（会话内按盐复用），解锁/免密会话不再重复执行 60 万次迭代，解锁响应明显提速；缓存仅在会话内存中，锁屏即清。
2. **SonderBus 事件总线**：新增 `event-bus.js`，store 数据变更统一发布事件，各页面订阅后自动重绘，替代分散的手工刷新钩子，跨模块数据变更即时可见。
3. **五子棋 AI 异步化**：AI 计算（含困难档前瞻）移至 `game-worker.js` Web Worker 执行，落子不卡界面；Worker 不可用时自动降级为同步计算；带过期应手丢弃（对局状态已变则旧应手作废）与重入守卫。
4. **持久化危机兜底**：localStorage 写失败且 IndexedDB 兜底亦失败时进入危机态——红色警示条（`qb-fail`，双主题配色）仅保留「导出备份」入口、隐藏其他操作；「知道了」关闭不持久化（重启后仍提示）；任一侧恢复后警示条自动消除并复位。
5. **测试基线**：新增 Worker 行为、过期应手、双写失败/恢复、危机态 UI 等回归，`npm test` 423 → **451 项全部通过**；tsc/lint 0；离线缓存同步至 v18。

### v5.2 工程防线补强与可靠性摘要（本版）

1. **表单注入转义**：`formModal` 的 `data-k` 属性值转义 + `collect` 改用 `getAttribute` 匹配，杜绝字段 key 注入属性选择器。
2. **测试时序加固（waitFor）**：`waitFor` 提取到 harness 并替换 4 处固定延时 wait——AI 落子/续走、IDB 迁移改为条件轮询，消除慢机器竞态。
3. **测试超时兜底**：`npm test` 增加 `--test-timeout=30000` 单测超时上限 + CI job 10min 硬性兜底，挂死测试不再拖垮流水线；移除遗留死 wait 定义。
4. **测试门闩（__SONDER_TEST__）**：`ctx/Pages/applyTheme` 等测试钩子隔离到测试进程，生产面只暴露 `store/render/idbReady`；harness 于脚本加载前注入门闩。
5. **死代码清理**：移除 `cloneGame`/`PRIORITY_LIST`/`GAME_KIND.ttt`/`DIFF_LABEL.mid` 及对应 d.ts 声明。
6. **重复逻辑收敛**：`num0`/`esc`/`hashStr`/`copyText` 收敛为共享实现，消除多文件重复。
7. **弱覆盖域补测**：新增 home/memo/consulting/news/design 五个页面交互测试（29 项，含删除确认 Promise 时序、任务勾选移出今日列表、收入负数拦截、危险链接 XSS 等）。
8. **CI 升级**：checkout/setup-node v4 → v5，消除 Node 20 弃用警告（CI 零 annotations）。
9. **PWA 导航 Network First**：导航请求改为网络优先（成功回写缓存、离线回退缓存首页），刷新即拿新版；静态资源保持 Cache First；离线缓存同步至 v19。
10. **测试基线**：新增回归后 `npm test` 451 → **512 项全部通过**（弱覆盖域 29 项 + 动态交互层契约与行为补测）；tsc/lint 0。

### v5.2 补丁摘要（P5b，紧随其后）

11. **file:// 直开误报解除**：直接用浏览器打开（file://）时，浏览器因 origin 为 null 拦截 manifest 等资源的加载（CORS/安全限制属环境噪音，非应用缺陷）；error-guard 对 file:// 环境下的资源类错误仅记录（`__sonderErrors` + 控制台）不再弹 toast，脚本异常与 Promise 拒绝不受影响仍照常提示；新增 2 项回归，`npm test` 512 → **514 项全部通过**；tsc/lint 0。

### v4.0 加固摘要（本版）

1. **契约防御体系**：
   - `globals.d.ts` 集中声明 Pages / SonderStore（61 个公开方法）/ UI 全局接口，`tsc --noEmit` 全绿（零构建类型检查真正生效）；
   - 新增 24 个领域记录类型 + SonderState/Settings/Summarize/Ctx 等接口，add/update/remove 全签名具体化，并修正 6 处与实现不符的错误签名；
   - `tests/contract.test.js` 守护接口形状与 schema；`tests/behavior.test.js` 12 条参数归一化/更新/删除语义契约；`tests/state.test.js` 5 条状态双轨契约（静态清单/重渲染幂等/变更可见/对局瞬态不残留）；
   - `tests/innerhtml.test.js` 审计全部 24 处 innerHTML 赋值点：12 清空 + 2 自动安全，其余 10 处进入人工审查白名单（逐条注明转义依据），双向校验防幽灵白名单。
2. **eslint@8 防线**：`npm run lint` 全绿，清理 28 处问题（4 error + 24 未用变量，含页面残留 currentCtx、games.js 死变量、store.js 死代码）。新增静态审计守卫：无注释空 catch 块与空回调禁止。
3. **错误上报体系**：`error-guard.js` 捕获未处理脚本错误/Promise 拒绝/资源加载失败，toast（3 秒合并防刷屏）+ 完整堆栈，记录保留于 `__sonderErrors` 供测试调试；store 两处静默 Promise 链升级为带 err 上报。
4. **XSS 属性注入修复**：`sanitizeUrl` 除协议白名单外转义引号与 &（防 href 属性闭合注入，news/design 两处拼接点自动免疫），新增全链路用例。
5. **加密 5 项修复**：锁屏致命 bug（内联 display:flex 覆盖 UA 的 [hidden] 规则导致解锁后锁屏永久盖住应用，改 style.display 直控 + 渲染级断言防假绿）；加密保存竞态（`_encChain` 串行队列保证密文按调用顺序落盘）；加密/明文边界四破口（锁定态 save 守卫、IDB 空回填用原始串、迁移走 _encSave 双写密文、锁定态拒绝导入）；未知加密版本密文保护。
6. **周报阅读时长修复**：`buildWeeklyReport` 按对象形态遍历 readingLog 与写入的数组形态不匹配（恒为 0），改为数组逐条累加并兼容旧对象形态。
7. **搜索 3 处修复**：多词查询跳转后任一词命中即定位高亮；跳高高亮从固定 80ms 延迟改渲染就绪轮询（消除快慢机器竞态）；`getIndex` 双 store 实例 _rev 失步隐患修掉。
8. **overlay 焦点管理（WCAG）**：打开自动落焦、Tab 焦点陷阱循环、关闭后焦点归还触发元素；keydown 监听器泄漏统一走 `_sonderClose` 清理；确认框/弹窗全路径回归。
9. **对比度达标 AA（≥4.5:1）**：浅色 --muted、pill 中低档、暗色 pill/overdue、配额条链接全部加深/加亮；`tests/contrast.test.js` 用 WCAG 相对亮度公式断言 8 组前景/背景组合。
10. **细节修复**：壁纸透明度滑块防抖（拖动仅预览、松手才落盘）；小游戏纪录统一并入 store（`miniRecords`，旧独立键一次性迁移）；今日计划日期筛选状态提升（进度卡与列表一致、切页保留）；Ai 越页劫持守卫 + 切回自动续走；切页后阅读时钟冻结修复（时钟链单链自愈）；旧数据缺字段链式崩溃补齐（clients.posts/posts.tags/news.tags/devProjects.tasks 等 9 处迁移）；扫雷首击 NaN 坐标崩溃修复 + 手机端棋盘适配（格子 ≥26px 触控、横向滚动容器）。
11. **store 领域拆分**：`store.js` 按领域拆出 `store-tasks / store-media / store-content / store-settings` 四个文件（1351 行减至约 960 行），API 静态导出不动，UMD 双环境兼容。
12. **脚本清单收敛**：index.html 为脚本唯一真源（tests/harness.js 自动解析），新增 `scripts/sync-sw.js`（npm run sync-sw 自动同步 sw 缓存清单并递增缓存版本，当前 v15）。

---

## 1. 产品目标和使用场景

### 产品目标
做个只供本人使用的工作与生活管理 App，把零散的个人工作（自媒体、开发、咨询、阅读、新闻、设计）和日常小事（今日计划、快速备忘）与休闲娱乐（六款小游戏）集中到一个本地工具里，减少多处切换、随手记录混乱的问题。

### 使用场景
- 电脑浏览器打开即用，快速查看今天要做什么。
- 临时有个想法，随手记入"快速备忘"。
- 每天按"今日计划"安排和处理任务，用 🍅 专注倒计时进入专注状态。
- 各工作模块分别记录进展，互不干扰，但首页可一览全局。
- 碎片时间在手机上玩五子棋、井字棋、猜数字、扫雷、猜成语或脑筋急转弯，战绩自动记录。

### 核心原则
- 纯前端实现：数据只保存在访问者自己的浏览器（localStorage）中，不上传任何服务器。
- 刷新、关闭标签页、关闭浏览器、重新启动后，数据都不能丢失。
- 不需要登录、云数据库、云同步，站点可离线打开运行。
- 中文界面，电脑 / 平板 / 手机浏览器均可使用。

---

## 2. 整体页面框架和导航结构

### 应用外壳
- **侧边导航**：应用名/Logo + 10 个导航入口，当前页高亮显示。
  - 首页总览、今日计划、自媒体、开发工作、咨询工作、阅读计划、看新闻计划、设计计划、娱乐游戏、数据与设置
  - 电脑：完整侧栏；平板（721–960px）：折叠为 70px 图标栏；手机竖屏（≤720px）：改为底部液态玻璃导航栏（可横向滑动、44px 触控目标、适配手势条安全区）。
- **顶部栏**：当前所在模块名称、当天日期、全局"＋"快捷新建按钮、全局"快速备忘"入口、全局搜索框。
- **内容区**：各模块独立渲染，切换导航时保留各页状态（娱乐游戏的对局状态切页后仍保留）。
- **弹窗统一规则**：新增/编辑/确认删除均使用居中浮层 + 半透明遮罩，按 Esc 可关闭；手机端为底部抽屉样式；焦点管理符合 WCAG（落焦/陷阱/归还）。
- **超宽屏**：桌面 >1240px 内容整体限宽 1240px 居中，大屏不摊开。

### 导航结构
```
侧边栏
├── 首页总览
├── 今日计划
├── 自媒体
├── 开发工作
├── 咨询工作
├── 阅读计划
├── 看新闻计划
├── 设计计划
├── 娱乐游戏
└── 数据与设置
```

---

## 3. 视觉与交互设计

- **主题**：水墨 × 液态玻璃。浅色「宣纸」/ 深色「墨黑」双主题，默认跟随系统 `prefers-color-scheme`，手动选择可覆盖并记住；磨砂玻璃卡片（backdrop-filter + 高光内边），朱砂红强调色，图表使用国画颜料色系；双主题文字/强调色 token 化（`--muted/--warn-text/--ok-text/--accent-text` 等），全部对比度 ≥4.5:1（WCAG AA，测试锁定）。
- **自定义壁纸**：设置页可上传背景图（≤2MB，超大图自动压缩），调节透明度（0–100%，默认 40%，实时预览并持久化，滑块防抖），可恢复默认；渲染使用独立 img 元素 + `object-fit: cover`，全机型铺满居中不拉伸。
- **微动态**：页面淡入错峰、图表水墨蔓延生长、按钮按压反馈、空状态呼吸、Toast 滑入、手机端弹窗底部抽屉。
- **性能优化**：移除 fixed 背景层级、磨砂降档适配（@supports 回退）、壁纸预加载、存储序列化去重、全局搜索索引缓存、困难五子棋前瞻剪枝——低端手机与老浏览器保证流畅。
- **无障碍**：尊重 `prefers-reduced-motion`；全键盘可达 + 弹层焦点管理（落焦/陷阱/归还）；图标按钮带 aria 标签与提示。
- **全平台适配**：
  - 电脑 >960px：完整侧栏 + 多列网格；>1240px 内容限宽居中
  - 平板 721–960px：侧栏为 70px 图标栏（隐藏文字）
  - 手机 ≤720px：底部液态玻璃导航
  - 超小屏 ≤360px：压缩留白不挤爆；`.row` 支持换行防溢出
  - 手机横屏：顶栏与导航自动变矮
  - 五子棋大棋盘 ≤720px：棋盘宽 `min(97vw,480px)`、格距 2px、内边距 8px、格子方形、棋子 86% 填充；≤360px 进一步压缩；棋盘始终不横向溢出（照顾 iPhone SE 一档小屏）
  - 扫雷棋盘手机端：棋盘宽 `max(100%, 列数×26px)` 横向滚动容器，格子 ≥26px 触控尺寸
  - 安全区 `env(safe-area-inset-bottom)`、`100vh→100dvh` 回退（老 iOS/安卓）、触控目标 ≥44px、输入控件 16px 防 iOS 聚焦缩放、`color-scheme` 同步表单与滚动条
- **PWA 友好**：`viewport-fit=cover` + `apple-mobile-web-app-capable`，支持「添加到主屏幕」作为 App 打开；Service Worker 离线缓存（当前缓存版本 v48，`npm run sync-sw` 自动同步清单并递增版本）；导航请求网络优先（刷新即拿新版，离线回退缓存首页），静态资源缓存优先。

---

## 4. 首页的内容结构

1. **顶部问候语 + 当天日期**；下方**每日金句**（库内按日期稳定轮换，每天 0 点自动换新；有机会随机展示自己的书摘；库内容缺失时优雅降级不报错）。
2. **今日任务摘要**：待办数/总数 + **今日完成率环形进度条**；可快捷打勾（不跳转页面）。
3. **快速备忘条**：直接输入即保存，最近一条常显在首页。
4. **各模块概览卡片区**（每模块一张数字卡片）：
   - 自媒体：选题数 / 待发布数
   - 开发工作：项目数 / 进行中任务数
   - 咨询工作：客户数 / 待跟进数
   - 阅读计划：在读数 / 总数
   - 看新闻计划：待读数 / 总数
   - 设计计划：项目数 / 进行中设计数
   - 娱乐游戏：对局数 / 胜平局
   - 点击任意卡片跳转对应模块。

---

## 5. 每个模块的用途

| 模块 | 用途 |
| --- | --- |
| 今日计划 | 管理每天要做的任务，按时间/优先级安排，专注倒计时，跟踪完成情况 |
| 快速备忘 | 任何时刻即点即存的文字记录，带保存时间，可查看历史 |
| 自媒体 | 管理内容创作：选题、月历排期、发布渠道、发布状态、阅读量/点赞与数据图表 |
| 开发工作 | 管理开发项目：任务、进度、Markdown 技术笔记 |
| 咨询工作 | 管理咨询客户与项目：客户档案、阶段、跟进记录、收入 |
| 阅读计划 | 管理书单：在读/想读/读完、阅读计时、读书笔记、书摘 |
| 看新闻计划 | 收藏想看的资讯：标题/链接/来源、待读/已读/收藏 |
| 设计计划 | 收集灵感、管理设计项目及阶段 |
| 娱乐游戏 | 井字棋 / 五子棋 / 猜数字 / 扫雷 / 猜成语 / 脑筋急转弯：AI 对决、双人对弈、悔棋、认输、自动战绩 |
| 数据与设置 | 统计总览、数据导出/备份/恢复、加密开关、主题与模块开关、通知与报告 |

---

## 6. 每个模块的数据和操作

### 今日计划
- 数据：任务标题、说明、优先级（四档四色，旧值自动迁移）、计划日期、完成状态、完成时间、排序、专注会话日志。
- 操作：新增、编辑、删除（二次确认）、勾选完成/恢复、调整顺序、开始专注（🍅 25 分钟悬浮倒计时，到时浏览器通知）。

### 快速备忘
- 数据：备忘内容、保存时间。
- 操作：即点即存、历史查看、归档。

### 自媒体
- 数据：选题/内容标题、平台账号、发布渠道（公众号/小红书/B 站/抖音）、标签、状态（草稿/待发布/已发布）、发布日期、阅读量/点赞、备注、进度。
- 操作：新增、编辑、筛选（标签/状态）、改状态、详情、**导出 CSV（含公式注入防护：= + - @ 开头字段加前缀）**、进度拖拽条、月历视图拖拽排期（桌面拖拽/移动端长按）、最近 5 篇阅读量 SVG 折线图、发布数据可视化。

### 开发工作
- 数据：项目名称、项目任务、进度、技术笔记（Markdown 渲染、代码片段一键复制）、完成统计。
- 操作：新增项目、项目内新增/勾选任务、写技术笔记（按更新时间倒序）、查看统计。

### 咨询工作
- 数据：客户档案、客户项目、项目阶段、跟进记录、待办、收入记录。
- 操作：新增客户、客户详情内管理项目/跟进/收入。

### 阅读计划
- 数据：书名、作者、状态（在读/想读/读完）、阅读进度、累计阅读分钟与会话日志、笔记摘录、书摘（句子 + 页码，独立「我的书摘」页按书分组）、完成日期。
- 操作：新增书、改状态、更新进度、开始/结束阅读计时、写摘录与书摘、按状态浏览、阅读量统计。

### 看新闻计划
- 数据：标题、链接、来源、标签、状态（待读/已读/收藏）。
- 操作：新增、点击打开链接、标记已读、收藏、按标签浏览。

### 设计计划
- 数据：灵感记录（链接/想法）、灵感分类、设计项目、项目阶段（构想/进行/定稿）。
- 操作：新增灵感、灵感分类浏览、新增项目、推进阶段。

### 娱乐游戏
- 数据：对局记录（游戏/模式/胜者/是否认输/AI 难度/备注/时间）、战绩汇总（对局数/胜/平）、小游戏最佳纪录（miniRecords 统一存储）。
- 操作：
  - 选游戏：井字棋 3×3 / 五子棋 15×15 / 猜数字 / 扫雷 / 猜成语 / 脑筋急转弯。
  - 对弈类（井字棋/五子棋）：模式 AI 对决（可执先手或后手）/ 双人对弈；三档 AI 难度（简单/普通/困难）持久化。
  - 落子：点击棋盘落子，AI 模式轮到 AI 时自动应手（首步延迟 320ms、后续 220ms，模拟思考节奏）。
  - 悔棋（AI 模式）：即点即悔无需确认，可连续悔多回合；AI 正在思考时悔棋会取消其待落子。
  - 悔棋（双人模式）：由刚落子的一方提出收回刚落的这一步，对方确认后才撤销；对局结束后不可悔棋。
  - 认输：二次确认，判对方胜；AI 模式判给 AI 获胜；胜者归属与战绩记录一致。
  - 新开局：有落子时二次确认并清空棋盘；连点新开局/切换模式不会弹多个确认框。
  - 迷你游戏：猜数字（7 次机会/大小提示/最佳纪录）、扫雷（三档难度/首击安全/首次插旗不布雷/死局可继续展开）、猜成语（3 次机会/提示字）、脑筋急转弯（看答案/奇趣问答）；战绩自动记录。
  - 返回选游戏：当前局未结束时二次确认；清空战绩二次确认。
- AI 行为：井字棋为完全搜索（必不输）；五子棋为评分启发式（主动五连优先、封堵对手冲四/活四、进攻权重高于防守，困难档带一层前瞻）。
- 对局切换导航后状态保留，回来可继续下（AI 被切页打断会自动续走）；战绩自动写入本地存储，首页概览与数据页统计同步。

### 数据与设置
- 数据：应用设置（主题、模块开关、动画帧率、通知开关、加密状态）、统计数据。
- 操作：切换主题（跟随系统/手动）、开关模块、启用/停用加密（向导 + 锁屏 + 免密会话）、上传/恢复壁纸、调透明度、生成本周报告（一键复制）、桌面通知授权、导出备份（未加密或加密密码）、导入恢复、查看统计（**完成率分母 = 今日到期任务：已完成 + 待办 + 过期**）、清空战绩、数据迁移（IndexedDB）。

---

## 7. 首页、今日计划和各模块之间的关系

- **今日计划**是每日任务的核心执行层；首页只展示其摘要并支持快捷打勾。
- **快速备忘**是全局随手记录入口；首页显示最近一条，完整历史在各模块/备忘录中。
- **各工作模块**是独立的记录单元；首页通过概览卡片汇总每个模块的关键数字，点击卡片进入。
- **娱乐游戏**为休闲模块：对局状态切换导航后保留（返回游戏页可继续），战绩计入首页与数据页统计。
- **数据与设置**控制全局外观、模块可见性以及数据备份恢复，不参与日常业务数据。
- 数据统一存储，各模块之间不强制关联；今日计划与工作模块不做自动联动。

---

## 8. 本地数据、备份和恢复要求

- **数据存储**：数据保存在当前浏览器内置的本地存储（localStorage）中，使用 JSON 结构；同时写入 IndexedDB 双写双存（容量约 5MB → 远超），启动时自动从较新版恢复并回填。
- **加密**：设置页可选启用存储加密（PBKDF2 60 万次 + AES-GCM-256）；启用后数据以密文存储，解锁密码只存在浏览器内存，不保存密码本身；**韧性防线**：锁定态禁止明文落盘、未知加密版本密文（如未来 v2）不被解析/覆盖、迁移与导入均走密文路径、保存按调用顺序串行加密；**性能**：会话内派生密钥缓存复用，解锁不再重复 60 万次迭代。
- **持久性要求**：刷新页面、关闭标签页、关闭并重新打开浏览器后，数据不丢（已有全量回归覆盖）。
- **备份**：在"数据与设置"中提供"导出备份"，将全部数据下载为本地 JSON 文件（加密状态下需输入解锁密码，导出文件为密文）。
- **恢复**：在"数据与设置"中提供"导入恢复"，选择备份文件恢复全部数据（导入前提示会覆盖现有数据；加密备份需输入密码；锁定态拒绝导入）。
- **说明**：备份文件由用户自行保存；不提供自动云备份；多设备数据各自独立，迁移用导出/导入。

---

## 9. 本版完成的功能

1. 侧边栏 + 顶部栏整体框架，10 个页面可切换，各页状态保留；顶栏全局搜索框（实时模糊匹配/分组/跳转高亮，索引覆盖书摘与游戏战绩）。
2. 手机底导 + 平板图标栏 + 桌面超宽屏（>1240px 限宽居中）全平台适配；安全区 / dvh / 触控 / 防缩放全齐。
3. 主题（宣纸/墨黑，跟随系统可覆盖）、自定义壁纸上传与透明度（0–100%，滑块防抖）、磨砂玻璃、微动态，老机型性能与降级；双主题对比度 WCAG AA。
4. 每日金句按日轮换（有机会展示自己的书摘），库缺失优雅降级。
5. 首页：问候、今日摘要 + 完成率环形进度条、快速备忘、各模块概览卡片、卡片跳转。
6. 今日计划：增删改/排序/勾选/分组、四档优先级四色圆点、🍅 专注 25 分钟悬浮倒计时（到时浏览器通知）、日期筛选状态跨页保留。
7. 一键备忘：即存即取、历史、归档。
8. 六个内容模块，各自增/删/改/筛选/统计；自媒体导出 CSV（防公式注入）+ 月历拖拽排期 + 渠道 + 阅读量折线图；开发笔记 Markdown 渲染与代码复制；阅读计时与「我的书摘」页；咨询收入记录。
9. 娱乐游戏六款：井字棋 / 五子棋（AI 三档难度 / 双人，悔棋认输开局确认制，全规则回归锁定）+ 猜数字 / 扫雷 / 猜成语 / 脑筋急转弯（战绩并入对战记录，含 solo 模式与备注、AI 难度显示；小游戏最佳纪录统一存储）。
10. 数据与设置：导出/导入/统计（完成率分母=今日到期任务）/清空战绩/主题/模块开关/动画帧率（60/90/120）/数据迁移（IndexedDB）/加密（向导+锁屏+免密会话+韧性防线）/桌面通知/本周报告一键复制/壁纸上传。
11. 统一交互：弹窗规则、二次确认、Esc 关闭、Toast、确认框防叠、弹层焦点管理（落焦/陷阱/归还）。
12. 可靠性：PWA 离线可用（缓存 v48 自动同步；导航网络优先，刷新即新版）、存储双写双存（集合级逐 key）、存储超 4.5MB 顶部警示条、双写后端同时失效时红色危机警示条强制引导导出、XSS 全模块净化（含属性注入与表单字段 key 转义）、可选加密、性能优化（单集合增量序列化/搜索索引缓存/AI 剪枝/派生密钥缓存）、错误上报与壳层降级。
13. 工程防线：eslint 全绿、零构建类型契约（d.ts + tsc）、innerHTML 白名单审计、状态双轨契约、store 领域拆分、脚本清单单一真源。
14. 自动化测试 592 项全绿（各版本全量验证）。

---

## 10. 明确不做 / 后续考虑的功能

- 登录、用户体系、权限、多人实时在线对战。
- 云同步、云数据库、多设备数据互通。
- 联网自动抓取新闻及任何联网功能；推送通知（仅限浏览器内桌面通知）。
- 今日计划与内容模块的自动联动。
- 国际化（多语言界面）——考虑中，尚未实现。

---

## 11. 验收标准（可实际检查）

1. 首页：问候语、日期、金句、今日摘要、完成率环形、备忘条、概览卡片全部正常。
2. 点卡片跳转对应模块；点导航可返回。
3. 今日计划/备忘及各模块核心操作可用且互不干扰；阅读进度、自媒体进度可实时。
4. 专注倒计时：开始后悬浮显示 25 分钟倒计时，到时弹浏览器通知（需授权）。
5. 阅读：开始阅读计时累计分钟；书摘页按书分组展示；标记读完自动记完成日期；首页金句位有机会看到自己的书摘。
6. 自媒体：月历视图可拖拽/长按选题排期；已发布卡片可填阅读量/点赞并出现在折线图；导出 CSV 中 = + - @ 开头字段已加防注入前缀。
7. 加密：设置启用向导 → 锁屏解锁 → 数据为密文 → 导出导入加密备份需密码；错密码不破坏密文；未启用时无感；手动写入未来版本密文（v2）后刷新仍提示解锁且密文原样保留。
8. 设置页完成率 = 今日到期任务完成数 /（已完成 + 待办 + 过期），与任务列表口径一致。
9. 娱乐游戏：六款游戏均可完成一局并把战绩记入对战记录（含 solo 模式、备注、AI 难度）；五子棋/井字棋的悔棋、认输、新开局均按规则跑通；终局后不能悔棋、再点认输不追加记录；连续点「新开局」只出现一个确认框；扫雷首击必安全（先插旗再翻开也安全、旗位保留）、死局可继续展开、胜利/失败判定正确。
10. 五子棋手机端：375px 宽手机打开为正方形格子、刚好在可视宽度内，无变形、无横向滚动；360px 级小屏仍可正常落子；扫雷手机端格子 ≥26px 可正常点击。
11. 超宽屏：桌面 2560px 宽打开，内容居中限宽 1240px，不左右摊开。
12. 平板 760px：侧栏收为图标栏；手机竖屏：底部导航+抽屉弹窗；横屏顶栏变矮。
13. 主题跟随系统切换（可手动覆盖）；壁纸上传生效、可调透明度、可恢复默认；双主题下正文/强调文字对比度 ≥4.5:1。
14. 导出 JSON → 修改数据 → 导入恢复，数据与导出时一致（导入前有覆盖提示）。
15. 持久化：任意数据录入后刷新、关标签、重启浏览器均不丢失。
16. 手机外链、离线打开本地 index.html 均可用；数据只在本地浏览器中。
17. 工程验收：npm test（592 项）、npm run typecheck、npm run lint 三绿。

---

## 12. 技术架构与质量保障

- **实现**：纯 HTML + CSS + 原生 JS，零构建、零运行时依赖；全部资源为相对路径，可部署到任意静态托管（Netlify / Cloudflare Pages / Vercel / GitHub Pages），也可直接双击打开本地文件。
- **代码组织**：
  - 数据层：`store.js`（核心：构造/持久化/加密/导入导出/汇总与共享 helper）+ `store-tasks / store-media / store-content / store-settings`（领域方法）+ `encryption.js`（可选加密核心：PBKDF2 60 万次 + AES-GCM-256，密文分支、锁屏、串行加密链、派生密钥缓存）+ `event-bus.js`（SonderBus 事件总线：store 变更发布 → 页面订阅自动重绘）
  - 组件：`ui.js`（弹窗/Toast/表单、统一 sanitize 净化与 URL 协议白名单、焦点管理）
  - 模块：`home / today / memo / selfmedia / dev / consulting / reading / news / design / settings` 各一个文件
  - 游戏：`games-logic.js`（纯对弈规则与三档难度 AI 策略：井字棋完全搜索、五子棋启发式评分 + 困难档前瞻剪枝；猜数字/扫雷/猜成语/脑筋急转弯规则与题库）+ `games-shared.js`（共享状态与 AI 调度，须最先加载）+ `games-mini.js`（猜数字/扫雷/猜成语/脑筋急转弯视图与纪录）+ `games-battle.js`（对弈视图、悔棋/认输/开局/战绩、AI 落子节奏与 Worker 调度）+ `games-view.js`（渲染纯函数）+ `games.js`（页面编排：render 分派/游戏选择/战绩区/测试钩子）+ `game-worker.js`（五子棋 AI 计算 Web Worker，异步不卡 UI；Worker 不可用时自动降级为同步计算，过期应手丢弃）
  - 搜索：`search.js`（全局索引 + 缓存、模糊匹配、分组跳转、水墨高亮定位）
  - 扩展渲染：`markdown.js`（技术笔记 Markdown 渲染与代码复制）
  - 壳层：`app.js`（路由/主题跟随/帧率档位/警示条/危机警示条/导航降级/离线状态指示）、`error-guard.js`（全局错误安全网与上报）、`sw.js`（Service Worker 离线缓存，当前 v45，脚本自动同步；导航网络优先）、`manifest.json`（PWA 清单与图标）
  - 类型：`globals.d.ts`（Pages/SonderStore 61 个公开方法/UI 全局接口 + 24 个领域类型契约）
- **主题系统**：CSS 自定义属性 + 深浅双主题；`@supports` 为不支持 `backdrop-filter` 的浏览器降低层级；壁纸独立 img 元素 + `object-fit: cover` + 透明度变量；文字/强调色 token 化并满足 WCAG AA。
- **数据兼容**：读取时做字段归一化（缺失补默认、非法值夹取 + 9 处旧字段迁移），支持历史版本数据平安迁移；IndexedDB 与 localStorage 双写双存，冲突按保存时间取新；加密开关对旧数据完全兼容（未启用时无感）；未来加密版本密文原样保留不破坏。
- **移动端棋盘细节**：15 列盘使用 grid；`.game-board.big` 手机断点压缩格距与边距、格子 `min-height: 0` 保持方正 1:1；宽度 `min(97vw, 480px)` 保证不溢屏；扫雷用 `max(100%, 列数×26px)` + 横向滚动容器保证格子 ≥26px；棋盘格子为游戏固有热区（整盘须尽收眼底），其余全部 UI 触控目标 ≥44px。
- **工程防线**：eslint@8（无注释空 catch 禁止）、零构建 TypeScript 类型检查（JSDoc + tsc --noEmit）、innerHTML 白名单审计（24 处赋值点全部验证）、契约测试（contract/behavior/state/innerhtml）、index.html 脚本单一真源（harness 自动解析）、`scripts/sync-sw.js` 自动同步 SW 清单并递增缓存版本。
- **自动化测试**：jsdom + `node --test`（glob 全量）+ fake-indexeddb；覆盖存储、加密（含竞态/边界/未来版本韧性/派生缓存）、集合级持久化（逐集合 key 读写/迁移/回填/加密 bundle）、UI、全部模块、样式、动效、壁纸、移动端自动适配、性能、游戏引擎（胜负检测/禁手逻辑/AI 策略/六款游戏/Worker 行为）、交互质量回归（确认弹窗防叠、悔棋语义、终局锁定、棋盘压缩）、PWA、搜索、离线状态指示、XSS 净化（含属性注入）、IndexedDB 双写与容量警示、持久化危机兜底、通知、对比度、CSS 变量契约、周报、本周报告，当前 **592 项全部通过**（`npm test` 全绿）。
- **持续质量**：每轮改动后执行源码 `node --check` 语法检查、`npm run typecheck`（零构建类型检查）、`npm run lint` 与全量测试；改动脚本/入口后运行 `npm run sync-sw` 同步离线缓存清单。