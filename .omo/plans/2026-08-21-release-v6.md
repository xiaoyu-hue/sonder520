# v6.0.0 Release Prep

## Goal
Ship v6.0.0 — finalize CHANGELOG, bump version, sync docs, push to production.

## Commits
1. `release: v6.0.0` — version bump + CHANGELOG + PRD sync

## Steps

### Step 1: Version Bump
- `package.json:3` — `"version": "1.0.0"` → `"6.0.0"`

### Step 2: CHANGELOG 收官
- Line 6: `## [v6.0] - 规划中（未发布）` → `## [v6.0] - 2026-08-21`
- Line 8: `### 已实施（工程基线，尚未发版）` → `### 已实施`
- Lines 92-118: Delete entire `### 计划` section (~27 lines) — moved to v6.1 backlog per user decision

### Step 3: PRD 同步
- Line 3: `> 版本：v6.0（规划中，工程基线）` → `> 版本：v6.0`
- Line 4: `> 状态：已完成开发并上线（v5.2 为最新发布版；v6.0 为渐进式框架演进，见 CHANGELOG）` → `> 状态：v6.0 为最新发布版（见 CHANGELOG）`

### Step 4: Verify
- `npm test` — 622 项全绿
- `npm run typecheck` — 零错误
- `npm run lint` — 零警告

### Step 5: Commit + Push
- `git commit -m "release: v6.0.0"`
- `git push origin main`
- 自动触发 CF Pages + GH Pages 部署

## Decisions
- CHANGELOG 计划 section 全部移入 v6.1 backlog（用户确认）
- package.json 版本从 1.0.0 → 6.0.0（用户确认）
