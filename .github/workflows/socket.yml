name: "Socket"

# 待定：官方集成需 SOCKET_SECURITY_API_KEY（注册 socket.dev 免费账号）。
# 注册后按官方文档恢复 push 触发；当前仅手动触发，避免 CI 红。
on:
  workflow_dispatch:

jobs:
  socket:
    name: Socket (supply-chain behavior scan)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

